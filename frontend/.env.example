# ── API Base URL ──────────────────────────────────────────────────────────────
# Points the axios client at the Express backend.
# Change only if you move the backend to a different host or port.
VITE_API_BASE_URL=http://localhost:5000/api
